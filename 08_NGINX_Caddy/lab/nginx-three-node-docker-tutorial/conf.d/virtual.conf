server {
    listen 80;
    server_name localhost;

    location / {
        root /home/student/html;
        index static.html;
    }
}

server {
    listen 3000;
    server_name localhost;

    location / {
        root /home/student/html;
        index secondhost.html;
    }
}
